# CheckMate

One-line: CheckMate — fake-news detection + real-time fact-checking.

Repository structure:
- backend/    (API + model serving)
- models/     (training scripts + checkpoints)
- data/       (raw and processed datasets)
- frontend/   (Next.js app)
- scripts/    (utility scripts: prepare dataset, etc.)
